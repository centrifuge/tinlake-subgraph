 // import { log } from "@graphprotocol/graph-ts"
 import poolConfigs from 'tinlake-pool-config';

// log.debug("will create new pool poolId {}", [poolConfigs || '']);
export const config = {
    poolMetas: []
}

